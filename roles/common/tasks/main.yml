---
# ========== Instalación de paquetes ==========

- import_tasks: install_update.yml
  become: true
  tags: [common, update]

- import_tasks: install_env_packages.yml
  become: true
  tags: [common, env]

- import_tasks: install_cargo.yml
  become: true
  become_user: "{{ target_user }}"
  tags: [common, cargo]

- import_tasks: install_extra_packages.yml
  become: true
  tags: [common, extra]

- import_tasks: install_zig.yml
  become: true
  tags: [common, zig]

- import_tasks: install_docker.yml
  become: true
  tags: [common, install, docker, infra]

- import_tasks: install_podman.yml
  become: true
  tags: [common, install, install_podman]

# ========== Configuración de sistema ==========

- import_tasks: user_podman_rootless.yml
  tags: [common, install, podman_rootless]

