---
- import_tasks: install_common.yml
  become: true
  tags: [terminal, install]

- import_tasks: config_root.yml
  become: true
  become_user: root
  tags: [terminal, config, root]

- import_tasks: config_alex.yml
  become: true
  become_user: alex
  tags: [terminal, config, alex]
