---
- import_tasks: install_chezmoi.yml
  become: true
  become_user: "{{ target_user }}"
  tags: [chezmoi, ch-install]

- import_tasks: user_chezmoi.yml
  become: true
  become_user: "{{ target_user }}"
  tags: [chezmoi, ch-apply]
