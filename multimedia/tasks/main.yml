---
- import_tasks: install_common.yml
  become: true
  tags: [multimedia, install]

- import_tasks: config_root.yml
  become: true
  become_user: root
  tags: [multimedia, config, root]

- import_tasks: config_alex.yml
  become: true
  become_user: alex
  tags: [multimedia, config, alex]
